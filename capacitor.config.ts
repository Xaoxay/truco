import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.xaoxay.truco', appName: 'Truco', webDir: 'dist',
  backgroundColor: '#123d32',
  ios: { contentInset: 'automatic' }
};
export default config;
